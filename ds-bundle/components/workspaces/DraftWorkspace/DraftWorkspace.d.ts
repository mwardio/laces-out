import * as React from 'react';

/**
 * DraftWorkspace — from @laces-out/web@0.1.0.
 */
export interface DraftWorkspaceProps {
  /* Takes no props — holds its own draft board state internally. */
}

export declare const DraftWorkspace: React.ComponentType<DraftWorkspaceProps>;
