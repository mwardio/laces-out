DemoRankingsStudio from @laces-out/web. Use via `window.LacesOut.DemoRankingsStudio` (bundle loaded from the root `_ds_bundle.js`).

Read-only tour of the rankings studio, backed by bundled demo data.

Ships its own dataset so it renders identically for every visitor with no
account, league, or network access. This is the marketing-site preview of the
real rankings studio — use it on public pages; use the live studio behind auth.

## Props

```ts
interface DemoRankingsStudioProps {
  /* Takes no props — renders the bundled demo rankings dataset. */
}
```
