import * as React from 'react';

/**
 * DemoRankingsStudio — from @laces-out/web@0.1.0.
 */
export interface DemoRankingsStudioProps {
  /* Takes no props — renders the bundled demo rankings dataset. */
}

export declare const DemoRankingsStudio: React.ComponentType<DemoRankingsStudioProps>;
