import * as React from 'react';

/**
 * PortfolioDashboard — from @laces-out/web@0.1.0.
 */
export interface PortfolioDashboardProps {
  /** Slot rendered directly beneath the overview row. */
  afterOverview?: React.ReactNode;
}

export declare const PortfolioDashboard: React.ComponentType<PortfolioDashboardProps>;
