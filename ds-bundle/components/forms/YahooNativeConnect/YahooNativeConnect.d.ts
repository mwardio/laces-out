import * as React from 'react';

/**
 * YahooNativeConnect — from @laces-out/web@0.1.0.
 */
export interface YahooNativeConnectProps {
  /* Takes no props — starts the Yahoo authorization handshake on click. */
}

export declare const YahooNativeConnect: React.ComponentType<YahooNativeConnectProps>;
