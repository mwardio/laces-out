import * as React from 'react';

/**
 * LacesOutMark — from @laces-out/web@0.1.0.
 */
export interface LacesOutMarkProps {
  /** Renders the smaller header variant. */
  compact?: boolean;
}

export declare const LacesOutMark: React.ComponentType<LacesOutMarkProps>;
